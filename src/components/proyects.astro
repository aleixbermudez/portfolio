---
const projects = [
  {
    title: "Cinetria",
    description:
      "Plataforma web de cine y series desarrollada con Laravel, integrada con la API de TMDB. Permite explorar películas y participar en un foro dinámico.",
    image: "/images/cinetria.jpg",
    link: "https://github.com/aleixbermudez/cinetria",
    tech: ["Laravel", "Bootstrap", "API", "JavaScript", "SQL"],
  },
  {
    title: "Construclan",
    description:
      "Sitio corporativo moderno y responsive para una empresa de construcción. Diseñado para comunicar con claridad servicios y proyectos.",
    image: "/images/construclan.jpg",
    link: "https://construclan.com",
    tech: ["HTML", "CSS", "Bootstrap"],
  }
]
---

<section id="projects" class="bg-white py-24 px-6 md:px-20">
  <div class="max-w-6xl mx-auto">
    <header class="text-center mb-16">
      <h2 class="text-5xl md:text-6xl font-extrabold text-gray-900 leading-tight">
        Proyectos
      </h2>
      <p class="text-gray-600 text-lg max-w-2xl mx-auto">
        Una muestra de mi trabajo: funcional, limpio y centrado en la experiencia del usuario.
      </p>
    </header>

    <div class="grid gap-10 sm:grid-cols-2 lg:grid-cols-3">
      {projects.map((project) => (
        <a
          href={project.link}
          target="_blank"
          rel="noopener noreferrer"
          class="group flex flex-col border border-gray-200 rounded-xl overflow-hidden hover:border-gray-300 hover:shadow-md transition-all duration-300 bg-white"
        >
          <div class="aspect-[16/10] overflow-hidden">
            <img
              src={project.image}
              alt={project.title}
              class="w-full h-full object-cover transition-transform duration-500 group-hover:scale-105"
            />
          </div>

          <div class="flex-1 p-5 flex flex-col justify-between space-y-4">
            <div>
              <h3 class="text-xl font-semibold text-gray-900 transition-colors">
                {project.title}
              </h3>
              <p class="text-gray-600 text-sm leading-relaxed mt-2">
                {project.description}
              </p>
            </div>

            <div class="flex flex-wrap gap-2 pt-2">
              {project.tech.map((t) => (
                <span class="text-xs bg-gray-100 text-gray-700 px-3 py-1 rounded-full font-medium">
                  {t}
                </span>
              ))}
            </div>
          </div>
        </a>
      ))}
    </div>
  </div>
</section>
